# TODO: confirm the real Probex dashboard host before deployment.
# The old value pointed at the retired IP over plain http (mixed-content on HTTPS).
# If the dashboard is served from this same domain, the line below is correct as-is;
# otherwise replace the host with the actual dashboard host (keep https).
NEXT_PUBLIC_PROBEX_URL=https://qubo-probex.duckdns.org/dashboard